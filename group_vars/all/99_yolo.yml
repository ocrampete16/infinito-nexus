# Connection settings for root user
ansible_user: root
ansible_become: false

# Dummy password since administrator user definition requires it but we won't use become
ansible_become_password: "dummy_not_used_for_root"

# Telegram bot configuration for sys-alm-telegram role
telegram_bot_token: "YOUR_BOT_TOKEN_HERE"  # Get this from @BotFather on Telegram
telegram_chat_id: "YOUR_CHAT_ID_HERE"     # Your Telegram chat IDection settings for root user
